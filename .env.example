# Copy to .env to override Docker Compose defaults. No secrets are required.
COMPOSE_PROJECT_NAME=compound-zero
API_PORT=8000
WEB_PORT=4173

# Public build-time browser configuration, not a secret. `/api` uses the
# same-origin reverse proxy supplied by the web container.
VITE_API_BASE_URL=/api

